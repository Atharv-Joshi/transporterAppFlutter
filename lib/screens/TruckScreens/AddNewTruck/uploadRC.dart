import 'package:flutter/material.dart';

class UploadRCScreen extends StatelessWidget {
  const UploadRCScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Text('upload RC Screen!'),
      ),
    );
  }
}
